	    /* user add to project */
	   /*
	    * start
	    * declare user object
	    * declare client object
	    * check if client has openings for project
	    * if user adds to client project add to clients user list
	    * if not return no more openingsc
	    * end
	    *
	    */

	   /* client accepts user */

	   /*
	    *  * start
	    * if user is added to client openings
	    * accept user as project creator
	    * else decline user from project
	    * end
	    */